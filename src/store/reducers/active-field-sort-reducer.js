import { initialState } from "../initialState";

export const activeFieldOfSortReducer = (state = initialState, action) => {
    switch (action.type) {
        default: 
            return state;
    }
};