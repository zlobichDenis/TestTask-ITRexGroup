import { initialState } from "../initialState.js";

export const currentIndexDataReducer = (state = initialState, action) => {
    switch (action.type) {
        default: 
            return state;
    }
};